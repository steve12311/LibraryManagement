<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dwtech.system.mapper.BookMapper">
    <insert id="insertIfAbsent">
        INSERT INTO lib_book (isbn, cover, name, intro, author, press_id, publish_time, category_id, price)
        VALUES (#{book.isbn}, #{book.cover}, #{book.name}, #{book.intro}, #{book.author},
                #{book.pressId}, #{book.publishTime}, #{book.categoryId}, #{book.price})
        ON DUPLICATE KEY UPDATE isbn = isbn
    </insert>
</mapper>
